package fr.ensicaen.ecole.genielogiciel.model;

import fr.ensicaen.ecole.genielogiciel.model.board.action.Move;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MoveTest {

    /*
    @Test
    void idle() {
        Move move = new Move();
        assertEquals(0, move._moveCount);
    }

    @Test
    void forward() {
        Move move = new Move(3);
        assertEquals(3, move._moveCount);
    }

    @Test
    void backward() {
        Move move = new Move(-2);
        assertEquals(-2, move._moveCount);
    }
    @Test
    void testSetMethod(){
        Move move = new Move(-2);
        move._moveCount = 5;
        assertEquals(5, move._moveCount);
    }
    */
}